import { Inter, Bitter } from "@next/font/google";

const inter = Inter({
  weight: 'variable',
  subsets: ["latin"],
})
const bitter = Bitter({
  weight: 'variable',
  subsets: ["latin"],
})

export { inter, bitter };
